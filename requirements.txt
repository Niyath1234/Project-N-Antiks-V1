# Core dependencies for SQL Analytics AI
ollama>=0.1.0
requests>=2.31.0
flask>=2.3.0

# For fine-tuning (install separately: pip install -r training/requirements.txt)
# torch, transformers, peft, datasets, accelerate, yt-dlp

